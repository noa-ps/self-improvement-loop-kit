# Interview dry-run

If install is missing, first turn is `silk install` only. No questions.

When install is done, first interview turn asks question 1 in chat: What should start this loop?

Not AskQuestion. Not a canvas. Not `http://127.0.0.1:8766`.

After nine answers, review, then `silk build` emit (intake json). Closing block:

- Run with client follow-playbook + `record-run`
- Success = probe
- Learn = later bullet in playbook `## Learned (probe-gated)`
- Evidence = `scoreboard.jsonl`
- Not memory
- Visualizer already in the skill; open later at `http://127.0.0.1:9348`

CLI interview is `silk build`.
